# tuples are like lists but can NOT be modified
# tuples use ()
# tuples cannot be sorted, extended, appended, revesed
# we can only do 'count' and 'index'
# tuples are more EFFICIENT
# tuples are more preferable as "TEMPORARY VARIABLES"

l = list()
print("LIST:",dir(l))

t = tuple()
print("TUPLE:",dir(t))